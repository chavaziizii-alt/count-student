# Excel Row Counter 📊

این ابزار ساده پایتون برای شمارش مجموع تمام ردیف‌ها (رکوردها) در چندین فایل اکسل طراحی شده است. این اسکریپت تمام شیت‌های (Sheets) هر فایل اکسل را بررسی کرده و مجموع نهایی را نمایش می‌دهد.

## ویژگی‌ها
- پشتیبانی از فایل‌های `.xlsx` و `.xls`
- شمارش خودکار تمام شیت‌های موجود در هر فایل
- نمایش مجموع ردیف‌ها برای هر فایل به صورت جداگانه
- نمایش مجموع کل نهایی

## پیش‌نیازها

برای اجرای این برنامه به پایتون نیاز دارید. ابتدا وابستگی‌ها را نصب کنید:
```bash
pip install -r requirements.txt

## نحوه استفاده

### روش ۱: شمارش فایل‌های کنار اسکریپت
فایل‌های اکسل را در کنار فایل `excel_counter.py` قرار دهید و دستور زیر را اجرا کنید:

bash
python excel_counter.py

### روش ۲: دادن آدرس پوشه خاص
اگر فایل‌های اکسل شما در پوشه دیگری هستند، مسیر آن را با فلگ `--path` به برنامه بدهید:

bash
python excel_counter.py --path "C:\Users\MyName\Documents\Excels"

## تکنولوژی‌ها
- Python 3
- Pandas
- Openpyxl


---

### ۴. فایل نادیده گرفتن (`.gitignore`)
این فایل بسیار مهم است. این فایل به Git می‌گوید که فایل‌های اکسلِ شما (که ممکن است اطلاعات محرمانه داشته باشند) و فایل‌های سیستمی پایتون را در گیت‌هاب آپلود نکند.

```text
# Python caches
__pycache__/
*.py[cod]

# Excel Files (Don't upload data!)
*.xlsx
*.xls
*.csv

# Environments
.env
venv/
env/
